# -*- coding: utf-8 -*-

from setuptools import setup,find_packages

setup(
      name= "src",
      version = "0.0.1",
      description = "its a test package",
      author = "vivek",
      packages = find_packages(),
      license = "MIT"
      
      
      )